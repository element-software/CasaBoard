// Reuse the shared Tailwind v4 PostCSS config from the monorepo package
// This makes Next.js (and Vercel) pick up the Tailwind plugin during build
export { default } from "@repo/tailwind-config/postcss";


